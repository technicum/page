<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>{{ settings.profile.name }}</title>
<link rel="stylesheet" href="/themes/biolink-creator/assets/css/style.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<style>
:root{
  --accent: {{ settings.profile.accent | default: '#e11d48' }};
  --ink: #1a1a18; --muted: #6b6b66; --border: #e5e5e2;
  --bg: #fafaf8; --white: #fff; --radius: 14px;
}
</style>
</head>
<body>
<div class="page">

  <!-- Hero -->
  {% assign pr = settings.profile %}
  <div class="hero">
    {% if pr.avatar != blank %}
      <img class="avatar" src="{{ pr.avatar }}" alt="{{ pr.name }}">
    {% else %}
      <div class="avatar-ph">{{ pr.name | slice: 0, 1 | upcase }}</div>
    {% endif %}
    <div class="creator-name">{{ pr.name }}</div>
    {% if pr.tagline %}<div class="tagline">{{ pr.tagline }}</div>{% endif %}
    {% if pr.bio %}<div class="bio">{{ pr.bio }}</div>{% endif %}
  </div>

  <!-- Socials accordion -->
  {% assign s = settings.socials %}
  {% if s.youtube != blank or s.instagram != blank or s.tiktok != blank or s.twitter != blank or s.spotify != blank or s.linkedin != blank or s.github != blank %}
  <div class="pz-sections" style="padding:0 16px;">
    <div class="pz-sec" id="sec-socials">
      <div class="pz-sec-head" onclick="pzToggle('sec-socials')">
        <div class="pz-sec-icon">📱</div>
        <div class="pz-sec-meta">
          <div class="pz-sec-title">{{ s.heading | default: 'Follow me' }}</div>
          <div class="pz-sec-sub">Connect on social media</div>
        </div>
        <div class="pz-sec-right">
          <svg class="pz-chevron" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="6 9 12 15 18 9"/></svg>
        </div>
      </div>
      <div class="pz-sec-body">
        <div class="pz-sec-inner" style="padding:20px 16px;text-align:center;">
          <div class="social-row">
            {% if s.youtube != blank %}<a class="soc-icon" href="{{ s.youtube }}" target="_blank" title="YouTube"><i class="fa-brands fa-youtube"></i></a>{% endif %}
            {% if s.instagram != blank %}<a class="soc-icon" href="{{ s.instagram }}" target="_blank" title="Instagram"><i class="fa-brands fa-instagram"></i></a>{% endif %}
            {% if s.tiktok != blank %}<a class="soc-icon" href="{{ s.tiktok }}" target="_blank" title="TikTok"><i class="fa-brands fa-tiktok"></i></a>{% endif %}
            {% if s.twitter != blank %}<a class="soc-icon" href="{{ s.twitter }}" target="_blank" title="Twitter"><i class="fa-brands fa-x-twitter"></i></a>{% endif %}
            {% if s.spotify != blank %}<a class="soc-icon" href="{{ s.spotify }}" target="_blank" title="Spotify"><i class="fa-brands fa-spotify"></i></a>{% endif %}
            {% if s.linkedin != blank %}<a class="soc-icon" href="{{ s.linkedin }}" target="_blank" title="LinkedIn"><i class="fa-brands fa-linkedin"></i></a>{% endif %}
            {% if s.github != blank %}<a class="soc-icon" href="{{ s.github }}" target="_blank" title="GitHub"><i class="fa-brands fa-github"></i></a>{% endif %}
          </div>
        </div>
      </div>
    </div>
  </div>
  {% endif %}

  <div class="body">

    <!-- Links (always visible) -->
    {% assign lk = settings.links %}
    {% if lk.items and lk.items.size > 0 %}
    <div class="links" style="margin-bottom:20px;">
      {% for item in lk.items %}
      <a class="link-btn {% if item.primary == 'yes' %}primary{% else %}secondary{% endif %}" href="{{ item.url }}" target="_blank">
        {% if item.emoji and item.primary != 'yes' %}<span class="link-emoji">{{ item.emoji }}</span>{% endif %}
        {{ item.label }}
      </a>
      {% endfor %}
    </div>
    {% endif %}

    <!-- Accordion: Featured Content -->
    {% assign fc = settings.featured %}
    {% if fc.items and fc.items.size > 0 %}
    <div class="pz-sections" style="padding:0;">
      <div class="pz-sec" id="sec-featured">
        <div class="pz-sec-head" onclick="pzToggle('sec-featured')">
          <div class="pz-sec-icon">🎬</div>
          <div class="pz-sec-meta">
            <div class="pz-sec-title">{{ fc.heading | default: 'Latest Content' }}</div>
            <div class="pz-sec-sub">{{ fc.items.size }} item{% if fc.items.size != 1 %}s{% endif %}</div>
          </div>
          <div class="pz-sec-right">
            <span class="pz-count">{{ fc.items.size }}</span>
            <svg class="pz-chevron" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="6 9 12 15 18 9"/></svg>
          </div>
        </div>
        <div class="pz-sec-body">
          <div class="pz-sec-inner">
            <div class="content-list">
              {% for item in fc.items %}
              <a class="content-card" href="{{ item.link }}" target="_blank">
                {% if item.thumbnail != blank %}<img class="content-thumb" src="{{ item.thumbnail }}" alt="{{ item.title }}">
                {% else %}<div class="content-thumb-ph">🎬</div>{% endif %}
                <div class="content-info">
                  {% if item.platform %}<div class="content-platform">{{ item.platform }}</div>{% endif %}
                  <div class="content-title">{{ item.title }}</div>
                </div>
              </a>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
    {% endif %}

    <!-- Contact actions -->
    {% assign c = settings.contact %}
    {% if c.email != blank or c.whatsapp != blank or c.phone != blank %}
    <div class="socials" style="margin-top:20px;margin-bottom:8px;">
      {% if c.email != blank %}<a class="soc-btn" href="mailto:{{ c.email }}"><i class="fa-solid fa-envelope"></i> Email</a>{% endif %}
      {% if c.whatsapp != blank %}<a class="soc-btn" href="https://wa.me/{{ c.whatsapp | remove: '+' | remove: ' ' }}" target="_blank"><i class="fa-brands fa-whatsapp"></i> WhatsApp</a>{% endif %}
      {% if c.phone != blank %}<a class="soc-btn" href="tel:{{ c.phone }}"><i class="fa-solid fa-phone"></i> Call</a>{% endif %}
    </div>
    {% endif %}

    <!-- Save Contact -->
    {% if c.email != blank or c.phone != blank or c.whatsapp != blank %}
    <div class="save-contact-bar">
      <button class="save-contact-btn" onclick="pzSaveContact()"><i class="fa-solid fa-user-plus"></i> {{ c.save_label | default: 'Save Contact' }}</button>
    </div>
    {% endif %}

  </div>

  <!-- Share -->
  <div class="share-bar">
    <button class="share-btn" onclick="pzShare()">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg>
      Share this page
    </button>
    <div class="share-toast" id="pzToast">✓ Link copied!</div>
  </div>

</div>
<script>
function pzToggle(id){var el=document.getElementById(id);el.classList.toggle('open');}
function pzSaveContact(){var n='{{ settings.profile.name | escape }}';var t='{{ settings.profile.tagline | default: "" | escape }}';var p='{{ settings.contact.phone | default: "" | escape }}';if(!p)p='{{ settings.contact.whatsapp | default: "" | escape }}';var e='{{ settings.contact.email | default: "" | escape }}';var w='{{ settings.contact.website | default: "" | escape }}';var vcf='BEGIN:VCARD\nVERSION:3.0\nFN:'+n+'\n'+(t?'TITLE:'+t+'\n':'')+(p?'TEL;TYPE=CELL:'+p+'\n':'')+(e?'EMAIL:'+e+'\n':'')+(w?'URL:'+w+'\n':'')+'URL:'+location.href+'\nEND:VCARD';var b=new Blob([vcf],{type:'text/vcard'});var a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=(n||'contact').replace(/\s+/g,'-')+'.vcf';a.click();}
function pzShare(){var u=location.href,t=document.title;if(navigator.share){navigator.share({title:t,url:u});}else{var ta=document.createElement('textarea');ta.value=u;document.body.appendChild(ta);ta.select();document.execCommand('copy');document.body.removeChild(ta);var el=document.getElementById('pzToast');el.classList.add('show');setTimeout(function(){el.classList.remove('show');},2200);}}
</script>
</body>
</html>
